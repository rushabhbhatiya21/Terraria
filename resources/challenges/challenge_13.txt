I briefly mentioned SetTextureFilter(a.second, TEXTURE_FILTER_POINT); at the end of the last lesson. Your task is to look into what these filtering modes are. 
If you Ctrl + click on TEXTURE_FILTER_POINT it will get you to where they are defined in code, and you will see all the options. Search online what they do. 
There is also a SetTextureWrap(a.second, TEXTURE_WRAP_REPEAT); function. Search what those things are as well!